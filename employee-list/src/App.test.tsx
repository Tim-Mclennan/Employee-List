import { describe, it, expect } from 'vitest';

// Initialising tests to make sure it works.
describe('Setup tests', () => {
  it('true test', () => {
    expect(true).toBe(true);
  });

  it('false test', () => {
    expect(false).toBe(false);
  });
});